import normalDemo from './normalDemo?raw'
import sourceCode from '../../../../packages/learn-utils/src/leetcode/isValid?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# isValid 有效的括号

[LeetCode 20. 有效的括号](https://leetcode.cn/problems/valid-parentheses/) 是栈的经典应用题。

## 题目描述

给定一个只包括 `'('`，`')'`，`'{'`，`'}'`，`'['`，`']'` 的字符串 s，判断字符串是否有效。

有效字符串需满足：
1. 左括号必须用相同类型的右括号闭合
2. 左括号必须以正确的顺序闭合

## 解题思路

### 栈 + 哈希表

1. **建立映射**：右括号映射到对应的左括号
2. **遍历字符串**：
   - 遇到左括号，入栈
   - 遇到右括号，检查栈顶是否匹配
3. **最终检查**：栈为空则有效

## 源码实现

<CodeOrSourceMdx language="typescript">
  {sourceCode}
</CodeOrSourceMdx>

## 使用示例

<CodeOrSourceMdx language="typescript">
  {normalDemo}
</CodeOrSourceMdx>

## 参数介绍

### s

- 类型：`string`
- 描述：只包含括号字符的字符串

## 返回值

- 类型：`boolean`
- 描述：括号是否有效

## 复杂度分析

- **时间复杂度**：O(n)，遍历一次字符串
- **空间复杂度**：O(n)，栈的最大深度

## 优化技巧

1. **长度判断**：奇数长度直接返回 false
2. **提前返回**：遇到不匹配立即返回

## 相关题目

- [最长有效括号](https://leetcode.cn/problems/longest-valid-parentheses/)
- [括号生成](https://leetcode.cn/problems/generate-parentheses/)
