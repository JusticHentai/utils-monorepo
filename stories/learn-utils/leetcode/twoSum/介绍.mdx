import normalDemo from './normalDemo?raw'
import anotherDemo from './anotherDemo?raw'
import sourceCode from '../../../../packages/learn-utils/src/leetcode/twoSum?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# twoSum 两数之和

[LeetCode 1. 两数之和](https://leetcode.cn/problems/two-sum/) 是 LeetCode 第一题，也是面试中最常见的算法题之一。

## 题目描述

给定一个整数数组 `nums` 和一个整数目标值 `target`，请你在该数组中找出和为目标值 `target` 的那两个整数，并返回它们的数组下标。

## 解题思路

### 哈希表解法（最优）

1. **核心思想**：遍历数组，对于每个元素，检查 `target - nums[i]` 是否在哈希表中
2. **时间复杂度**：O(n)，只需一次遍历
3. **空间复杂度**：O(n)，哈希表存储

### 暴力解法

1. 两层循环，枚举所有组合
2. 时间复杂度 O(n²)，不推荐

## 源码实现

<CodeOrSourceMdx language="typescript">
  {sourceCode}
</CodeOrSourceMdx>

## 示例1

<CodeOrSourceMdx language="typescript">
  {normalDemo}
</CodeOrSourceMdx>

## 示例2

<CodeOrSourceMdx language="typescript">
  {anotherDemo}
</CodeOrSourceMdx>

## 参数介绍

### nums

- 类型：`number[]`
- 描述：整数数组

### target

- 类型：`number`
- 描述：目标和

## 返回值

- 类型：`[number, number] | []`
- 描述：两个数的索引，找不到返回空数组

## 复杂度分析

<table>
  <thead>
    <tr>
      <th>方法</th>
      <th>时间复杂度</th>
      <th>空间复杂度</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>哈希表</td>
      <td>O(n)</td>
      <td>O(n)</td>
    </tr>
    <tr>
      <td>暴力法</td>
      <td>O(n²)</td>
      <td>O(1)</td>
    </tr>
  </tbody>
</table>

## 相关题目

- [三数之和](https://leetcode.cn/problems/3sum/)
- [四数之和](https://leetcode.cn/problems/4sum/)
