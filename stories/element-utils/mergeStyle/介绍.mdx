import basicDemo from './basicDemo?raw'
import arrayDemo from './arrayDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# mergeStyle

合并多个 React CSSProperties 样式对象。

## 合并样式对象

<CodeOrSourceMdx language="typescript">
  {basicDemo}
</CodeOrSourceMdx>

## 合并数组样式

<CodeOrSourceMdx language="typescript">
  {arrayDemo}
</CodeOrSourceMdx>

## 参数介绍

### args

- 类型：`Args[]`
- 描述：可接受多种类型的参数
  - `CSSProperties`：样式对象
  - `CSSProperties[]`：样式对象数组
  - `null | undefined | false`：会被过滤掉

### 返回值

- 类型：`CSSProperties`
- 描述：合并后的样式对象，后面的样式会覆盖前面的同名属性
