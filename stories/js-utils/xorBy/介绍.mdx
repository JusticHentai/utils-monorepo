import xorByDemo from './xorByDemo?raw'
import edgeCaseDemo from './edgeCaseDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# xorBy

类似于 xor，但可以指定迭代器

## 详细介绍

`xorBy` 类似于 xor，但可以指定迭代器。

**常用场景**：
- 数据处理和转换
- 功能组合和增强
- 简化常见操作

## 基础用法

点击按钮查看 `xorBy` 的基本使用效果。

<CodeOrSourceMdx language="typescript">
  {xorByDemo}
</CodeOrSourceMdx>

## 边界情况验证

验证各种边界条件下的行为表现。

<CodeOrSourceMdx language="typescript">
  {edgeCaseDemo}
</CodeOrSourceMdx>

## 参数介绍

<table>
  <thead>
    <tr>
      <th>参数名</th>
      <th>类型</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>arrays</td>
      <td><code>unknown</code></td>
      <td>要处理的数组</td>
    </tr>
    <tr>
      <td>iteratee</td>
      <td><code>unknown</code></td>
      <td>迭代器函数</td>
    </tr>
  </tbody>
</table>

### 返回值

- 类型：`unknown`
- 描述：返回新的对称差集数组

## 具体实现原理

1. **文件职责**: `xorBy/index.ts` 导出 xorBy 函数
2. **核心流程**: 类似于 xor，但可以指定迭代器
3. **关键技术点**: 使用 Math 内置方法
4. **数据流向**: 接收参数 → 处理逻辑 → 返回结果
