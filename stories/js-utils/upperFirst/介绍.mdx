import upperFirstDemo from './upperFirstDemo?raw'
import edgeCaseDemo from './edgeCaseDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# upperFirst

将字符串首字母转换为大写，其余部分保持不变

## 详细介绍

`upperFirst` 将字符串首字母转换为大写，其余部分保持不变。

**常用场景**：
- 数据处理和转换
- 功能组合和增强
- 简化常见操作

## 基础用法

点击按钮查看 `upperFirst` 的基本使用效果。

<CodeOrSourceMdx language="typescript">
  {upperFirstDemo}
</CodeOrSourceMdx>

## 边界情况验证

验证各种边界条件下的行为表现。

<CodeOrSourceMdx language="typescript">
  {edgeCaseDemo}
</CodeOrSourceMdx>

## 参数介绍

<table>
  <thead>
    <tr>
      <th>参数名</th>
      <th>类型</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>str</td>
      <td><code>unknown</code></td>
      <td>要转换的字符串</td>
    </tr>
  </tbody>
</table>

### 返回值

- 类型：`string`
- 描述：返回首字母大写的字符串

## 具体实现原理

1. **文件职责**: `upperFirst/index.ts` 导出 upperFirst 函数
2. **核心流程**: 将字符串首字母转换为大写，其余部分保持不变
3. **关键技术点**: 直接计算并返回结果
4. **数据流向**: 接收参数 → 处理逻辑 → 返回结果
