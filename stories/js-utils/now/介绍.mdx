import nowDemo from './nowDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# now

获取当前时间的 Unix 时间戳（毫秒）。

## 详细介绍

`now` 获取当前时间的 Unix 时间戳（毫秒），内部调用 `Date.now()`。

**常用场景**：
- 性能计时
- 生成时间戳 ID
- 时间差计算

## 获取当前时间戳

使用 `now` 获取当前的 Unix 时间戳。

<CodeOrSourceMdx language="typescript">
  {nowDemo}
</CodeOrSourceMdx>

## 参数介绍

<table>
  <thead>
    <tr>
      <th>参数名</th>
      <th>类型</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>

  </tbody>
</table>

### 返回值

- 类型：`number`
- 描述：当前时间的 Unix 时间戳（毫秒）

## 具体实现原理

1. **文件职责**: `index.ts` 封装 Date.now()
2. **核心流程**: 调用 Date.now() 返回时间戳
3. **关键技术点**: 使用原生 Date.now() API
4. **数据流向**: 调用 → Date.now() → 返回时间戳数值
