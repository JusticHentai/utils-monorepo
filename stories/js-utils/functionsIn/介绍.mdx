import functionsInDemo from './functionsInDemo?raw'
import edgeCaseDemo from './edgeCaseDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# functionsIn

返回对象所有函数属性名数组（含继承属性）

## 详细介绍

`functionsIn` 返回对象所有函数属性名数组（含继承属性）。

**常用场景**：
- 数据处理和转换
- 功能组合和增强
- 简化常见操作

## 基础用法

点击按钮查看 `functionsIn` 的基本使用效果。

<CodeOrSourceMdx language="typescript">
  {functionsInDemo}
</CodeOrSourceMdx>

## 边界情况验证

验证各种边界条件下的行为表现。

<CodeOrSourceMdx language="typescript">
  {edgeCaseDemo}
</CodeOrSourceMdx>

## 参数介绍

<table>
  <thead>
    <tr>
      <th>参数名</th>
      <th>类型</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>object</td>
      <td><code>unknown</code></td>
      <td>要检查的对象</td>
    </tr>
  </tbody>
</table>

### 返回值

- 类型：`string[]`
- 描述：返回函数属性名数组

## 具体实现原理

1. **文件职责**: `functionsIn/index.ts` 导出 functionsIn 函数
2. **核心流程**: 返回对象所有函数属性名数组（含继承属性）
3. **关键技术点**: 使用 typeof 运算符进行类型检查
4. **数据流向**: 接收参数 → 处理逻辑 → 返回结果
