import bindKeyDemo from './bindKeyDemo?raw'
import edgeCaseDemo from './edgeCaseDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# bindKey

创建一个函数，在 object[key] 上调用方法，可以预设参数

## 详细介绍

`bindKey` 创建一个函数，在 object[key] 上调用方法，可以预设参数。

**常用场景**：
- 数据处理和转换
- 功能组合和增强
- 简化常见操作

## 基础用法

点击按钮查看 `bindKey` 的基本使用效果。

<CodeOrSourceMdx language="typescript">
  {bindKeyDemo}
</CodeOrSourceMdx>

## 边界情况验证

验证各种边界条件下的行为表现。

<CodeOrSourceMdx language="typescript">
  {edgeCaseDemo}
</CodeOrSourceMdx>

## 参数介绍

<table>
  <thead>
    <tr>
      <th>参数名</th>
      <th>类型</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>object</td>
      <td><code>unknown</code></td>
      <td>绑定的对象</td>
    </tr>
    <tr>
      <td>key</td>
      <td><code>unknown</code></td>
      <td>方法的键名</td>
    </tr>
    <tr>
      <td>partials</td>
      <td><code>unknown</code></td>
      <td>预设的参数</td>
    </tr>
  </tbody>
</table>

### 返回值

- 类型：`unknown`
- 描述：返回新的绑定函数

## 具体实现原理

1. **文件职责**: `bindKey/index.ts` 导出 bindKey 函数
2. **核心流程**: 创建一个函数，在 object[key] 上调用方法，可以预设参数
3. **关键技术点**: 使用 typeof 运算符进行类型检查
4. **数据流向**: 接收参数 → 处理逻辑 → 返回结果
