import flatMapDepthDemo from './flatMapDepthDemo?raw'
import edgeCaseDemo from './edgeCaseDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# flatMapDepth

映射集合并将结果扁平化指定深度

## 详细介绍

`flatMapDepth` 映射集合并将结果扁平化指定深度。

**常用场景**：
- 数据处理和转换
- 功能组合和增强
- 简化常见操作

## 基础用法

点击按钮查看 `flatMapDepth` 的基本使用效果。

<CodeOrSourceMdx language="typescript">
  {flatMapDepthDemo}
</CodeOrSourceMdx>

## 边界情况验证

验证各种边界条件下的行为表现。

<CodeOrSourceMdx language="typescript">
  {edgeCaseDemo}
</CodeOrSourceMdx>

## 参数介绍

<table>
  <thead>
    <tr>
      <th>参数名</th>
      <th>类型</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>collection</td>
      <td><code>unknown</code></td>
      <td>要迭代的集合</td>
    </tr>
    <tr>
      <td>iteratee</td>
      <td><code>unknown</code></td>
      <td>每次迭代调用的函数</td>
    </tr>
    <tr>
      <td>depth</td>
      <td><code>unknown</code></td>
      <td>扁平化深度，默认为 1</td>
    </tr>
  </tbody>
</table>

### 返回值

- 类型：`unknown`
- 描述：返回新的扁平化数组

## 具体实现原理

1. **文件职责**: `flatMapDepth/index.ts` 导出 flatMapDepth 函数
2. **核心流程**: 映射集合并将结果扁平化指定深度
3. **关键技术点**: 使用循环遍历
4. **数据流向**: 接收参数 → 处理逻辑 → 返回结果
