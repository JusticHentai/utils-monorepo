import findLastIndexDemo from './findLastIndexDemo?raw'
import edgeCaseDemo from './edgeCaseDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# findLastIndex

类似 findIndex，但从右到左遍历数组

## 详细介绍

`findLastIndex` 类似 findIndex，但从右到左遍历数组。

**常用场景**：
- 数据转换和过滤
- 集合查询和检索
- 条件聚合计算

## 基础用法

点击按钮查看 `findLastIndex` 的基本使用效果。

<CodeOrSourceMdx language="typescript">
  {findLastIndexDemo}
</CodeOrSourceMdx>

## 边界情况验证

验证各种边界条件下的行为表现。

<CodeOrSourceMdx language="typescript">
  {edgeCaseDemo}
</CodeOrSourceMdx>

## 参数介绍

<table>
  <thead>
    <tr>
      <th>参数名</th>
      <th>类型</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>array</td>
      <td><code>unknown</code></td>
      <td>要搜索的数组</td>
    </tr>
    <tr>
      <td>predicate</td>
      <td><code>unknown</code></td>
      <td>每次迭代调用的函数</td>
    </tr>
    <tr>
      <td>fromIndex</td>
      <td><code>unknown</code></td>
      <td>搜索的起始索引，默认为 array.length - 1</td>
    </tr>
  </tbody>
</table>

### 返回值

- 类型：`unknown`
- 描述：返回匹配元素的索引，否则返回 -1

## 具体实现原理

1. **文件职责**: `findLastIndex/index.ts` 导出 findLastIndex 函数
2. **核心流程**: 类似 findIndex，但从右到左遍历数组
3. **关键技术点**: 使用 Math 内置方法
4. **数据流向**: 接收参数 → 处理逻辑 → 返回结果
