import zipObjectDeepDemo from './zipObjectDeepDemo?raw'
import edgeCaseDemo from './edgeCaseDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# zipObjectDeep

类似于 zipObject，但支持属性路径

## 详细介绍

`zipObjectDeep` 类似于 zipObject，但支持属性路径。

**常用场景**：
- 数据处理和转换
- 功能组合和增强
- 简化常见操作

## 基础用法

点击按钮查看 `zipObjectDeep` 的基本使用效果。

<CodeOrSourceMdx language="typescript">
  {zipObjectDeepDemo}
</CodeOrSourceMdx>

## 边界情况验证

验证各种边界条件下的行为表现。

<CodeOrSourceMdx language="typescript">
  {edgeCaseDemo}
</CodeOrSourceMdx>

## 参数介绍

<table>
  <thead>
    <tr>
      <th>参数名</th>
      <th>类型</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>paths</td>
      <td><code>unknown</code></td>
      <td>属性路径数组</td>
    </tr>
    <tr>
      <td>values</td>
      <td><code>unknown</code></td>
      <td>对应的值数组</td>
    </tr>
  </tbody>
</table>

### 返回值

- 类型：`string[]`
- 描述：返回新对象

## 具体实现原理

1. **文件职责**: `zipObjectDeep/index.ts` 导出 zipObjectDeep 函数
2. **核心流程**: 类似于 zipObject，但支持属性路径
3. **关键技术点**: 使用循环遍历
4. **数据流向**: 接收参数 → 处理逻辑 → 返回结果
