import hasInDemo from './hasInDemo?raw'
import edgeCaseDemo from './edgeCaseDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# hasIn

检查对象属性路径是否存在（含继承属性）

## 详细介绍

`hasIn` 检查对象属性路径是否存在（含继承属性）。

**常用场景**：
- 数据处理和转换
- 功能组合和增强
- 简化常见操作

## 基础用法

点击按钮查看 `hasIn` 的基本使用效果。

<CodeOrSourceMdx language="typescript">
  {hasInDemo}
</CodeOrSourceMdx>

## 边界情况验证

验证各种边界条件下的行为表现。

<CodeOrSourceMdx language="typescript">
  {edgeCaseDemo}
</CodeOrSourceMdx>

## 参数介绍

<table>
  <thead>
    <tr>
      <th>参数名</th>
      <th>类型</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>object</td>
      <td><code>unknown</code></td>
      <td>要检查的对象</td>
    </tr>
    <tr>
      <td>path</td>
      <td><code>unknown</code></td>
      <td>要检查的路径</td>
    </tr>
  </tbody>
</table>

### 返回值

- 类型：`boolean`
- 描述：如果路径存在，返回 true

## 具体实现原理

1. **文件职责**: `hasIn/index.ts` 导出 hasIn 函数
2. **核心流程**: 检查对象属性路径是否存在（含继承属性）
3. **关键技术点**: 使用循环遍历
4. **数据流向**: 接收参数 → 处理逻辑 → 返回结果
