import isMatchWithDemo from './isMatchWithDemo?raw'
import edgeCaseDemo from './edgeCaseDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# isMatchWith

类似于 isMatch，但支持自定义比较器

## 详细介绍

`isMatchWith` 类似于 isMatch，但支持自定义比较器。

**常用场景**：
- 条件判断前的类型检查
- 数据验证和过滤
- 类型安全的分支处理

## 基础用法

点击按钮查看 `isMatchWith` 的基本使用效果。

<CodeOrSourceMdx language="typescript">
  {isMatchWithDemo}
</CodeOrSourceMdx>

## 边界情况验证

验证各种边界条件下的行为表现。

<CodeOrSourceMdx language="typescript">
  {edgeCaseDemo}
</CodeOrSourceMdx>

## 参数介绍

<table>
  <thead>
    <tr>
      <th>参数名</th>
      <th>类型</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>object</td>
      <td><code>unknown</code></td>
      <td>要检查的对象</td>
    </tr>
    <tr>
      <td>source</td>
      <td><code>unknown</code></td>
      <td>要匹配的属性值</td>
    </tr>
    <tr>
      <td>customizer</td>
      <td><code>unknown</code></td>
      <td>自定义比较函数</td>
    </tr>
  </tbody>
</table>

### 返回值

- 类型：`unknown`
- 描述：如果 object 匹配，返回 true

## 具体实现原理

1. **文件职责**: `isMatchWith/index.ts` 导出 isMatchWith 函数
2. **核心流程**: 类似于 isMatch，但支持自定义比较器
3. **关键技术点**: 使用 typeof 运算符进行类型检查
4. **数据流向**: 接收参数 → 处理逻辑 → 返回结果
