import someDemo from './someDemo?raw'
import edgeCaseDemo from './edgeCaseDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# some

检测集合中是否有任何元素满足谓词函数

## 详细介绍

`some` 检测集合中是否有任何元素满足谓词函数。

**常用场景**：
- 数据转换和过滤
- 集合查询和检索
- 条件聚合计算

## 基础用法

点击按钮查看 `some` 的基本使用效果。

<CodeOrSourceMdx language="typescript">
  {someDemo}
</CodeOrSourceMdx>

## 边界情况验证

验证各种边界条件下的行为表现。

<CodeOrSourceMdx language="typescript">
  {edgeCaseDemo}
</CodeOrSourceMdx>

## 参数介绍

<table>
  <thead>
    <tr>
      <th>参数名</th>
      <th>类型</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>collection</td>
      <td><code>unknown</code></td>
      <td>要迭代的集合</td>
    </tr>
    <tr>
      <td>predicate</td>
      <td><code>unknown</code></td>
      <td>每次迭代调用的函数</td>
    </tr>
  </tbody>
</table>

### 返回值

- 类型：`unknown`
- 描述：如果有元素通过谓词检查，返回 true，否则返回 false

## 具体实现原理

1. **文件职责**: `some/index.ts` 导出 some 函数
2. **核心流程**: 检测集合中是否有任何元素满足谓词函数
3. **关键技术点**: 使用循环遍历
4. **数据流向**: 接收参数 → 处理逻辑 → 返回结果
