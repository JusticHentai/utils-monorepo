import uniqueIdDemo from './uniqueIdDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# uniqueId

生成递增的唯一 ID。

## 详细介绍

`uniqueId` 生成递增的唯一 ID，可指定前缀。

**常用场景**：
- 临时 DOM 元素 ID
- 组件 key 生成
- 唯一标识符

## 生成唯一ID

使用 `uniqueId` 生成递增的唯一标识符。

<CodeOrSourceMdx language="typescript">
  {uniqueIdDemo}
</CodeOrSourceMdx>

## 参数介绍

<table>
  <thead>
    <tr>
      <th>参数名</th>
      <th>类型</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>prefix</td>
      <td><code>string</code></td>
      <td>可选前缀，默认空字符串</td>
    </tr>
  </tbody>
</table>

### 返回值

- 类型：`string`
- 描述：唯一 ID 字符串

## 具体实现原理

1. **文件职责**: `index.ts` 实现递增 ID 生成
2. **核心流程**: 维护内部计数器 → 每次调用递增 → 拼接前缀和计数器
3. **关键技术点**: 闭包/模块级变量保存计数状态
4. **数据流向**: 前缀 → 计数器递增 → 拼接 → 返回 ID 字符串
