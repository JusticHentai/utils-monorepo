import curryRightDemo from './curryRightDemo?raw'
import edgeCaseDemo from './edgeCaseDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# curryRight

类似 curry，但从右向左接受参数

## 详细介绍

`curryRight` 类似 curry，但从右向左接受参数。

**常用场景**：
- 数据处理和转换
- 功能组合和增强
- 简化常见操作

## 基础用法

点击按钮查看 `curryRight` 的基本使用效果。

<CodeOrSourceMdx language="typescript">
  {curryRightDemo}
</CodeOrSourceMdx>

## 边界情况验证

验证各种边界条件下的行为表现。

<CodeOrSourceMdx language="typescript">
  {edgeCaseDemo}
</CodeOrSourceMdx>

## 参数介绍

<table>
  <thead>
    <tr>
      <th>参数名</th>
      <th>类型</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>func</td>
      <td><code>unknown</code></td>
      <td>要柯里化的函数</td>
    </tr>
    <tr>
      <td>arity</td>
      <td><code>unknown</code></td>
      <td>需要提供给 func 的参数数量，默认为 func.length</td>
    </tr>
  </tbody>
</table>

### 返回值

- 类型：`unknown`
- 描述：返回柯里化后的函数

## 具体实现原理

1. **文件职责**: `curryRight/index.ts` 导出 curryRight 函数
2. **核心流程**: 类似 curry，但从右向左接受参数
3. **关键技术点**: 直接计算并返回结果
4. **数据流向**: 接收参数 → 处理逻辑 → 返回结果
