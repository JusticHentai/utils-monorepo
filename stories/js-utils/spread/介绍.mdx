import spreadDemo from './spreadDemo?raw'
import edgeCaseDemo from './edgeCaseDemo?raw'
import { CodeOrSourceMdx } from '@storybook/addon-docs/blocks'

# spread

创建一个函数，将数组参数展开传递给原函数

## 详细介绍

`spread` 创建一个函数，将数组参数展开传递给原函数。

**常用场景**：
- 数据处理和转换
- 功能组合和增强
- 简化常见操作

## 基础用法

点击按钮查看 `spread` 的基本使用效果。

<CodeOrSourceMdx language="typescript">
  {spreadDemo}
</CodeOrSourceMdx>

## 边界情况验证

验证各种边界条件下的行为表现。

<CodeOrSourceMdx language="typescript">
  {edgeCaseDemo}
</CodeOrSourceMdx>

## 参数介绍

<table>
  <thead>
    <tr>
      <th>参数名</th>
      <th>类型</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>func</td>
      <td><code>unknown</code></td>
      <td>要展开参数的函数</td>
    </tr>
    <tr>
      <td>start</td>
      <td><code>unknown</code></td>
      <td>展开开始的位置</td>
    </tr>
  </tbody>
</table>

### 返回值

- 类型：`unknown`
- 描述：返回新的函数

## 具体实现原理

1. **文件职责**: `spread/index.ts` 导出 spread 函数
2. **核心流程**: 创建一个函数，将数组参数展开传递给原函数
3. **关键技术点**: 直接计算并返回结果
4. **数据流向**: 接收参数 → 处理逻辑 → 返回结果
