/**
 * 解析时间 返回结果类型
 */
export interface ParseTimeResult {
  time: number // 时间戳
  year: number // 年
  month: number // 月
  day: number // 日
  hour: number // 小时
  minutes: number // 分钟
  second: number // 秒
}
